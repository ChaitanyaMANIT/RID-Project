# Jungle Trail (optional)

`jungle-01.jpg`, `jungle-02.jpg`

Chapter 4 plays an animated rebuild of the fall by default. To use real photos
instead, open `src/data/jungleTrail.ts` and set:

```ts
media: 'photos',
```

Photos support an optional caption: see that same file's `captions` array.